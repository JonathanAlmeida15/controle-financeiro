# -----------------------
# DATABASE CONFIG
# -----------------------
spring.datasource.url=jdbc:mysql://localhost:3306/fullstack?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# -----------------------
# SECURITY / JWT
# -----------------------
app.jwt.secret=MEGA-CHAVE-SECRETA-ALTERE-ISTO
app.jwt.expiration=86400000  # 1 dia

# -----------------------
# CORS
# -----------------------
app.cors.allowed-origins=http://localhost:5173
